---
'@norges-domstoler/dds-components': patch
---

Fikser bug der flere `<DatePicker>` kalendere kunne være åpnet samtidig, og at klikking på en annen `<DatePicker>` ikke lukket kalender i den første.
