---
'@norges-domstoler/dds-components': patch
---

Fikser bredden på inputsegmentene i `<DatePicker>` og `<TimePicker>`, slik at det ikke hopper når brukeren skriver verdien. Legger også padding på hvert segment, så de ser romsligere ut ved fokus. Reduserer samtidig spacing mellom saparator (. eller :) og segment. `<DatePicker>` og `<TimePicker>` har dermed litt mindre bredde i de fleste størrelsene.
