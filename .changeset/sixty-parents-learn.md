---
'@norges-domstoler/dds-components': minor
---

Støtte for `iconState` prop i `<Icon>` og `<Button>` for animerte ikoner. Hvis ikonet satt i `icon` prop støtter animasjon via states vil `iconState` prop bli støttet med dedikerte verdier for det spesifikke ikonet.
