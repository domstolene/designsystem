---
'@norges-domstoler/dds-components': minor
---

Støtte for ulike formål i `<ToggleBar>` med prop `purpose`. Den tar inn `'primary'` eller `'secondary'` med standardverdi `'primary'`. Styling er den samme hvis standerdverdien brukes.
