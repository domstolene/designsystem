---
'@norges-domstoler/dds-components': minor
---

Legger på spacing mellom komponent og `<InputMessage>` i alle komponenter som støtter `errorMessage` prop. Dette grunnet bruk av `dds-border-radius-surface` på alle hjørner i `<InputMessage>`. Kan påvirke layout hos konsumenten.
