---
'@norges-domstoler/dds-components': minor
---

Støtte for `clearable` prop i `<NativeSelect>`; brukeren kan trykke en knapp til å sette komponenten til tom verdi. Krever at første `<option>` har `value=""`.
