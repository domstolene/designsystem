import {
    colors,
    font,
    border,
    borderRadius,
    spacing,
    grid,
    innerShadow,
    outerShadow
    } from './dds/index';

const ddsTokens = {
    colors,
    font,
    border,
    borderRadius,
    spacing,
    grid,
    innerShadow,
    outerShadow
};

export default ddsTokens;