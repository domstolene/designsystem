import {
    colors,
    font,
    border,
    borderRadius,
    spacing,
    innerShadow,
    outerShadow
    } from './dds/index';

const ddsTokens = {
    colors,
    font,
    border,
    borderRadius,
    spacing,
    innerShadow,
    outerShadow
};

export default ddsTokens;