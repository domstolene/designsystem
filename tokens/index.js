import {
    colors,
    font,
    border,
    borderRadius,
    spacing,
    grid,
    innerShadow,
    outerShadow,
    fontPackages
    } from './dds/index';

const ddsTokens = {
    colors,
    font,
    border,
    borderRadius,
    spacing,
    grid,
    innerShadow,
    outerShadow,
    fontPackages
};

export default ddsTokens;