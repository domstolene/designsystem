import {
  colors,
  font,
  border,
  borderRadius,
  spacing,
  grid,
  innerShadow,
  outerShadow,
  fontPackages,
} from "../dds/index";

export const ddsBaseTokens = {
  colors,
  font,
  border,
  borderRadius,
  spacing,
  grid,
  innerShadow,
  outerShadow,
  fontPackages,
};
