import {
  colors,
  font,
  border,
  borderRadius,
  spacing,
  grid,
  innerShadow,
  outerShadow,
  fontPackages,
} from "../dds";

export const ddsBaseTokens = {
  colors,
  font,
  border,
  borderRadius,
  spacing,
  grid,
  innerShadow,
  outerShadow,
  fontPackages,
};
