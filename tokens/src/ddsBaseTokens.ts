import {
  colors,
  font,
  border,
  borderRadius,
  spacing,
  iconSizes,
  grid,
  innerShadow,
  outerShadow,
  fontPackages,
} from "../dds";

export const ddsBaseTokens = {
  colors,
  font,
  border,
  borderRadius,
  spacing,
  iconSizes,
  grid,
  innerShadow,
  outerShadow,
  fontPackages,
};
