import { Meta, ArgsTable, PRIMARY_STORY } from '@storybook/addon-docs';
import { Checkbox, CheckboxGroup } from '.';

<Meta title="Design system/Checkbox/API" component={Checkbox} />

## Checkbox

En avkrysningboks.

<ArgsTable story={PRIMARY_STORY} />
I tillegg støttes alle native HTML-attributter som er en del av `InputHTMLAttributes<HTMLInputElement>`-interface.

## CheckboxGroup

Wrapper for en `<Checkbox />` gruppe. Brukes ved flere barn.

<ArgsTable of={CheckboxGroup} />
I tillegg støttes alle native HTML-attributter som er en del av `HTMLAttributes<HTMLDivElement>`-interface.
