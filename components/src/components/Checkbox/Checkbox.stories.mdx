import { Meta, ArgsTable, PRIMARY_STORY } from '@storybook/addon-docs';
import { Checkbox, CheckboxGroup } from '.';

<Meta title="Design system/Checkbox/Docs" component={Checkbox} />

<ArgsTable story={PRIMARY_STORY} />
I tillegg støttes alle native HTML-attributter som er en del av `InputHTMLAttributes<HTMLInputElement>`-interface, f.eks. `id`, `autoFocus` osv.

<ArgsTable of={CheckboxGroup} />
I tillegg støttes alle native HTML-attributter som er en del av `HTMLAttributes<HTMLDivElement>`-interface, f.eks. `id` osv.
