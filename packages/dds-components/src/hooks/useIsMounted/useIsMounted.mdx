import { Canvas, Controls, Meta } from '@storybook/addon-docs/blocks';
import {
  ComponentLinkRow,
  Source,
} from '@norges-domstoler/storybook-components';
import * as useIsMountedStories from './useIsMounted.stories';

<Meta of={useIsMountedStories} />

# useIsMounted

<ComponentLinkRow
  docsHref="
  582fac-useismounted"
  githubHref={{ folder: 'hooks', comp: 'useIsMounted' }}
/>

`@returns` om komponenten er mounted.

<Canvas of={useIsMountedStories.Preview} sourceState="none" />

## Bruk

<Source code={`
import { useIsMounted } from '@norges-domstoler/dds-components';

function MyComponent() {

    const isMounted = useIsMounted();

    useEffect(() => {
      if(isMounted()) {
      // logikk når mounted
      }
    }, [isMounted]);

};`} />
