import { Canvas, Controls, Meta } from '@storybook/addon-docs/blocks';
import {
  ComponentLinkRow,
  Source,
} from '@norges-domstoler/storybook-components';
import * as useCombinedRefStories from './useCombinedRef.stories';

<Meta of={useCombinedRefStories} />

# useCombinedRef

<ComponentLinkRow
  docsHref="537981-usecombinedref"
  githubHref={{ folder: 'hooks', comp: 'useCombinedRef' }}
/>

Kombinerer `ref`s for et element.

`@returns` callback med kombinerte `ref`s.

<Canvas of={useCombinedRefStories.Preview} sourceState="none" />
<Controls of={useCombinedRefStories.Preview} />

## Bruk

<Source code={`
import { useCombinedRef } from '@norges-domstoler/dds-components';

function MyComponent(ref) {

    const itemRef = useRef<HTMLDivElement>(null);
    const combinedRef = useCombinedRef(ref, itemRef);

    return <div ref={combinedRef}>innhold</div>

};`} />
