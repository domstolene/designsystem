import { Canvas, Controls, Meta } from '@storybook/addon-docs/blocks';
import { ComponentLinkRow } from '@norges-domstoler/storybook-components';
import * as ButtonGroupStories from './ButtonGroup.stories';

<Meta of={ButtonGroupStories} />

# ButtonGroup

<ComponentLinkRow
  docsHref="478fdf-buttongroup"
  figmaHref="node-id=24184-33314"
  githubHref={{ folder: 'components', comp: 'ButtonGroup' }}
  storybookHref={{ folder: 'components', comp: 'buttongroup' }}
/>

`<ButtonGroup>` er en wrapper for en gruppe med knapper. Den skal ha `<Button>` som barn, og den håndterer følgende props for barna: `purpose` og `size`. Ellers brukes knappene som vanlig.

## Props

<Canvas of={ButtonGroupStories.Preview} />
<Controls of={ButtonGroupStories.Preview} />

## Retningslinjer

- Bruk gjerne `aria-label` eller `aria-labelledby` for å beskrive hele gruppen for assisterende verktøy.
- Hvis komponenten brukes som en toolbar, bruk `role="toolbar"`.
