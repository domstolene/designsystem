import {
  Canvas,
  Controls,
  Meta,
  ArgTypes,
  Story,
} from '@storybook/addon-docs/blocks';
import { Paper } from '.';
import {
  Source,
  ComponentLinkRow,
} from '@norges-domstoler/storybook-components';
import * as PaperStories from './Paper.stories';

<Meta of={PaperStories} />

# Paper

<ComponentLinkRow
  docsHref="5922aa-paper"
  githubHref={{ folder: 'components', comp: 'layout/Paper' }}
  storybookHref={{ folder: 'layout-primitives', comp: 'paper' }}
/>

`<Paper>` er en code-only layout primitive; et grunnelement som brukes som byggekloss for layout, samt det visuelle for en flate: bakgrunnsfarge, border, heving og border radius.
Den støtter design tokens aliases direkte via props.

## Props

<Canvas of={PaperStories.Preview} />
<Controls of={PaperStories.Preview} />
