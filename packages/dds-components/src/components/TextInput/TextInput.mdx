import { Meta, Canvas, Controls } from '@storybook/blocks';
import { ComponentLinkRow } from '@norges-domstoler/storybook-components';
import * as TextInputStories from './TextInput.stories';

<Meta of={TextInputStories} />

# TextInput

<ComponentLinkRow
  docsHref="https://design.domstol.no/987b33f71/p/8048eb-textinput/b/800765"
  figmaHref="https://www.figma.com/file/ewqSDmkgqDQ5PyOsRp4V5b/?node-id=155%3A38"
  githubHref="https://github.com/domstolene/designsystem/tree/main/packages/components/src/components/TextInput"
  storybookHref="https://domstolene.github.io/designsystem/?path=/story/dds-components-textinput--default"
/>

## Props

<Canvas of={TextInputStories.Default} sourceState="shown" />
<Controls of={TextInputStories.Default} />

### maxLength

Når native attributtet `maxLength` brukes dukker det opp en tegnteller til høyre under inputfeltet:

<Canvas of={TextInputStories.WithCharacterCount} sourceState="shown" />

For å ikke vise tegntelleren sett `withCharacterCounter=false`.

## Retningslinjer

### Maks antall tegn

Maks antall tegn i input bør ta utgangspunkt i konteksten til applikasjonen, så ta gjerne en vurdering. Som default kan det maksimale antallet være så mange tegn som applikasjonen støtter; for eksempel, hvis databasen støtter opptil 255 tegn for dette feltet, kan du sette maks lengde på input til 255 tegn.

### Placeholder

Unngå å bruke `placeholder` attributtet. Hvis du bruker den istedenfor semantisk ledetekst ser brukeren ikke hva feltet ber om etter det blir fylt ut. I tillegg kan inputfeltet se ferdig fylt ut.

### Affiks

Prefix/suffix skal ikke være en erstatter for en beskrivende label. Den leses ikke av skjermleser, så label må beskrivende nok for formålet.

### Generelt

- Bruk gjerne `autofocus` for å indikere hvor brukeren skal starte.
- Bruk `onChange` slik at brukeren kan få umiddelbar tilbakemelding på deres input baser på validering.
- Det kan være lurt å lagre kladd av input i skjemaet brukeren fyller ut.
