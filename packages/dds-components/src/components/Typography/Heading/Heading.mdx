import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import { ComponentLinkRow } from '@norges-domstoler/storybook-components';
import { Link } from '..';
import * as HeadingStories from './Heading.stories';

<Meta of={HeadingStories} />

# Heading

<ComponentLinkRow
  docsHref="76b220-heading"
  figmaHref="node-id=24240-41889"
  githubHref={{ folder: 'components', comp: 'Typography/Heading' }}
  storybookHref={{ folder: 'components', comp: 'typography-heading' }}
/>

`<Heading>` er en komponent som tilsvarer `<h1>`-`<h6>` HTML tag. Den bruker typografistiler definert i Figma til å gi tekst riktig styling.

## Props

<Canvas of={HeadingStories.Preview} />
<Controls of={HeadingStories.Preview} />

## Med spacing

`<Heading>` har en variant med margin og padding som er tilpasset bruk i artikler o.l., der overskriften plasseres over en `<Paragraph>`. Den sørger for at avstanden fra tekst over er større enn fra tekst under.

<Canvas of={HeadingStories.WithMargins} />
