import { ArgTypes, Canvas, Controls, Meta } from '@storybook/addon-docs/blocks';
import { Accordion, AccordionBody, AccordionHeader } from '.';
import {
  ComponentLinkRow,
  Source,
} from '@norges-domstoler/storybook-components';
import * as AccordionStories from './Accordion.stories';
import meta from './Accordion.stories';
import { Tabs, Tab, TabList, TabPanel, TabPanels } from '../Tabs';
import { Divider } from '../Divider';

<Meta of={meta} />

# Accordion

<ComponentLinkRow
  docsHref="648734-accordion-"
  figmaHref="node-id=17381-2896"
  githubHref={{ folder: 'components', comp: 'Accordion' }}
  storybookHref={{ folder: 'components', comp: 'accordion' }}
/>

`<Accordion>` er en komponent som kan utvide og trekke sammen innhold.

## Delkomponenter

<Tabs>
  <TabList>
    <Tab>Accordion</Tab>
    <Tab>AccordionHeader</Tab>
    <Tab>AccordionBody</Tab>
  </TabList>

  <TabPanels>
    <TabPanel>
      En container for delkomponenter som håndterer oppførsel og universell utforming. Den skal ha:

      - `<AccordionHeader>` som første barn,
      - `<AccordionBody>` som andre barn.

      En gruppe med `<Accordion>` legges i en `<div>` container.

      <Canvas of={AccordionStories.Preview} />

      <Controls of={AccordionStories.Preview} />
    </TabPanel>
    <TabPanel>

      Header i `<Accordion>` som utvider og trekker sammen innhold. Skal brukes som første barn og den får automatisk riktig id fra `<Accordion>`. Komponenten har default typografi som kan bli overskrevet ved å bruke typografikomponenter som barn.

      <ArgTypes of={AccordionHeader} />

    </TabPanel>
    <TabPanel>
      Body i `<Accordion>` som kan utvides og trekkes sammen. Skal brukes som andre barn og den får automatisk riktig `id` fra `<Accordion>`. Komponenten har default typografi som kan bli overskrevet ved å bruke typografikomponenter som barn.

      <ArgTypes of={AccordionBody} />
    </TabPanel>

  </TabPanels>
</Tabs>

<Divider />

## Custom

Du kan bygge egen accordion ved hjelp av `useAccordion` hook. Da bruker du egne elementer for header og innhold, mens hooken håndterer oppførsel og universell uftorming.

Du trenger å sette følgende props fra `useAccordion`:

- `height` fra `bodyProps`som `height` i `style` eller tilsvarende, samt resten av `bodyProps` på din innhold-wrapper. `bodyProps` inneholder også `className`, kombiner den med din CSS.
- `headerProps` på din header. Den bør være en knapp for riktig semantikk.
- `bodyContentRef` på container inni din innhold-wrapper. Den sørger for animasjon.

<Canvas of={AccordionStories.Custom} />

### Types

<Source code={`
export interface AccordionHeaderProps {
  onClick: () => void;
  'aria-expanded': boolean;
  'aria-controls': string;
  id: string;
}

export interface AccordionBodyProps {
  id: string;
  role: string;
  'aria-labelledby': string;
  'aria-hidden': boolean;
  height?: number;
  className?: string;
}

export interface AccordionState {
  id: string;
  isExpanded: boolean;
  setIsExpanded: Dispatch<SetStateAction<boolean>>;
  openAccordion: () => void;
  closeAccordion: () => void;
  toggleExpanded: () => void;
  headerProps: AccordionHeaderProps;
  bodyProps: AccordionBodyProps;
  bodyContentRef: RefObject<HTMLDivElement>;
}
`} />

## Retningslinjer

- Ikke legg interaktive elementer (knapp, radio, inputfelt...) inni `<AccordionHeader>`. Den er allerede en knapp og kan ikke ha nøstet interaksjon.
- Ikke bruk `<AccordionHeader>` til å kontrollere andre elementer, som å åpne `<OverflowMenu>` eller en modal.
