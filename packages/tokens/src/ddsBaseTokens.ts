import {
  border,
  borderRadius,
  breakpoints,
  colors,
  font,
  fontPackages,
  grid,
  iconSizes,
  innerShadow,
  outerShadow,
  spacing,
} from '../dds';

export const ddsBaseTokens = {
  colors,
  font,
  breakpoints,
  border,
  borderRadius,
  spacing,
  iconSizes,
  grid,
  innerShadow,
  outerShadow,
  fontPackages,
};
