import {
  colors,
  font,
  breakpoints,
  border,
  borderRadius,
  spacing,
  iconSizes,
  grid,
  innerShadow,
  outerShadow,
  fontPackages,
} from "../dds";

export const ddsBaseTokens = {
  colors,
  font,
  breakpoints,
  border,
  borderRadius,
  spacing,
  iconSizes,
  grid,
  innerShadow,
  outerShadow,
  fontPackages,
};
